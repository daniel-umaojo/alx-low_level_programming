#include <stdio.h>
#include <stdlib.h>
/**
 * main- Entry point
 * * Description:C file that reads alphabet
 * Return:Always 0 (success)
*/
int main(void)
{
	char c;

	for (c = '0'; c <= '9'; c++)
		putchar(c);
	putchar('\n');
	return (0);
}
